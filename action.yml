name: 'HTML Preview'
description: 'Preview HTML page in PR'
inputs:
  html_file:
    description: 'The HTML file you want to preview'
    required: true
    default: 'index.html'
runs:
  using: 'node12'
  main: 'index.js'
